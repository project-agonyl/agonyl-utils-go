# shuecombinationfile

Reads and writes aligned 0x20-byte `ZoneData/item/ShueCombinationData` recipes. All sixteen recovered little-endian fields are addressable without filesystem or runtime concerns.
