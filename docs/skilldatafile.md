# skilldatafile

`skilldatafile` owns one class-specific 63-row skill file. Each row is 157 bytes and retains its full binary representation.

The package exposes recovered header bytes, six monster-rate bytes, and three level sections. Writers require exactly 63 rows.
