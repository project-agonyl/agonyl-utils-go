# hiredsoldierskillfile

`hiredsoldierskillfile` owns one class-specific `HSST` file. Each record is 720 bytes with ten 71-byte level sections.

The package exposes only the recovered item, skill-point, money, and lore costs. All other bytes remain opaque and round-trip unchanged.
